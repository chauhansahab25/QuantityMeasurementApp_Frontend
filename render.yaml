services:
  - type: static
    name: quantity-measurement-frontend
    buildCommand: "npm install && npm run build"
    staticPublishPath: dist/quantity-measurement
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: 18
